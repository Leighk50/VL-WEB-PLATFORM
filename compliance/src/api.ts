export const api=async<T=any>(path:string,options:RequestInit={})=>{const token=localStorage.getItem('compliance_token');const r=await fetch('/api'+path,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) as any,...options.headers}});const body=await r.json().catch(()=>({}));if(!r.ok)throw new Error(body.error||'Request failed');return body as T};
