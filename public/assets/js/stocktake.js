(()=>{
  const panel=document.getElementById('panel-stocktake');if(!panel)return;
  const search=document.getElementById('stockSearch');
  const suggestions=document.getElementById('stockSuggestions');
  const form=document.getElementById('stockNewItem');
  const rows=document.getElementById('stockRows');
  const status=document.getElementById('stockStatus');
  const meta=document.getElementById('stockMeta');
  let data={items:[],types:[],locations:[]};
  let activeId=null;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  async function api(url,opts={}){const r=await fetch(url,{credentials:'same-origin',...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});const j=await r.json().catch(()=>({}));if(r.status===401){location.href='/admin';throw new Error('Session expired.');}if(!r.ok){const e=new Error(j.error||`Request failed (${r.status})`);e.data=j;throw e;}return j;}
  function total(item){return (item.counts?.bar||0)+(item.counts?.cellar||0)+(item.counts?.walk_in_fridge||0);}
  function renderRows(){
    const items=[...data.items].sort((a,b)=>a.type.localeCompare(b.type)||a.description.localeCompare(b.description));
    if(!items.length){rows.innerHTML='<div class="admin-card"><p>No master stock items yet. Start typing above to add the first one.</p></div>';return;}
    rows.innerHTML=`<div class="admin-card" style="overflow:auto"><table style="width:100%;border-collapse:collapse;min-width:820px"><thead><tr><th style="text-align:left">Type</th><th style="text-align:left">Description</th><th style="text-align:left">Size</th><th>Bar</th><th>Cellar</th><th>Walk-in Fridge</th><th>Total</th></tr></thead><tbody>${items.map(i=>`<tr data-stock-id="${esc(i.id)}" style="border-top:1px solid #ddd"><td>${esc(i.type)}</td><td><strong>${esc(i.description)}</strong></td><td>${esc(i.size)}</td><td><input type="number" min="0" step="0.01" data-loc="bar" value="${esc(i.counts?.bar||0)}" style="width:86px"></td><td><input type="number" min="0" step="0.01" data-loc="cellar" value="${esc(i.counts?.cellar||0)}" style="width:86px"></td><td><input type="number" min="0" step="0.01" data-loc="walk_in_fridge" value="${esc(i.counts?.walk_in_fridge||0)}" style="width:110px"></td><td data-total style="text-align:center;font-weight:700">${total(i)}</td></tr>`).join('')}</tbody></table></div>`;
  }
  function fillForm(item){activeId=item?.id||null;form.type.value=item?.type||'';form.description.value=item?.description||search.value.trim();form.size.value=item?.size||'';document.getElementById('stockAddBtn').textContent=activeId?'Use Existing Item':'Add New Master Item';}
  function renderSuggestions(list){
    if(!list.length){suggestions.innerHTML=search.value.trim()?'<div class="admin-card"><p>No existing match. Complete the fields below to add this as a new master item.</p></div>':'';fillForm(null);return;}
    suggestions.innerHTML=`<div class="admin-card"><strong>Existing products</strong><div style="margin-top:8px">${list.map(i=>`<button type="button" class="small-btn" data-stock-pick="${esc(i.id)}" style="margin:4px 6px 4px 0">${esc(i.description)} · ${esc(i.size)} <small>(${esc(i.type)})</small></button>`).join('')}</div></div>`;
  }
  let timer=null;
  search?.addEventListener('input',()=>{clearTimeout(timer);const q=search.value.trim();if(!q){suggestions.innerHTML='';fillForm(null);return;}timer=setTimeout(async()=>{try{const r=await api('/api/admin/stocktake/search?q='+encodeURIComponent(q));renderSuggestions(r.results||[]);}catch(e){status.textContent=e.message;}},180);});
  suggestions?.addEventListener('click',e=>{const btn=e.target.closest('[data-stock-pick]');if(!btn)return;const item=data.items.find(i=>i.id===btn.dataset.stockPick);if(item){fillForm(item);search.value=item.description;suggestions.innerHTML=`<div class="admin-card"><p><strong>Selected:</strong> ${esc(item.description)} · ${esc(item.size)}. Update its quantities in the table below.</p></div>`;document.querySelector(`[data-stock-id="${CSS.escape(item.id)}"]`)?.scrollIntoView({behavior:'smooth',block:'center'});}});
  rows?.addEventListener('input',e=>{const input=e.target.closest('[data-loc]');if(!input)return;const row=input.closest('[data-stock-id]');const item=data.items.find(i=>i.id===row.dataset.stockId);if(!item)return;item.counts=item.counts||{};item.counts[input.dataset.loc]=Math.max(0,Number(input.value)||0);row.querySelector('[data-total]').textContent=total(item);});
  async function load(){try{status.textContent='Loading stock…';data=await api('/api/admin/stocktake');meta.textContent=`${data.items.length} master products${data.updatedAt?' · Last saved '+new Date(data.updatedAt).toLocaleString():''}`;renderRows();status.textContent='';if(form.type.options.length<=1){for(const t of data.types){const o=document.createElement('option');o.value=t;o.textContent=t;form.type.appendChild(o);}}}catch(e){status.textContent=e.message;}}
  async function addItem(e){e.preventDefault();if(activeId){status.textContent='Existing product selected. Enter its counts in the table below.';return;}const body={type:form.type.value,description:form.description.value.trim(),size:form.size.value.trim()};try{status.textContent='Checking master list…';const result=await api('/api/admin/stocktake/items',{method:'POST',body:JSON.stringify(body)});status.textContent=`Added ${result.item.description} to the master list.`;search.value='';suggestions.innerHTML='';form.reset();activeId=null;await load();}catch(err){if(err.data?.suggestions?.length){renderSuggestions(err.data.suggestions);status.textContent='Similar products already exist. Choose one above, or click “Add as new anyway” if this is definitely a different product.';const btn=document.getElementById('stockForceAdd');btn.hidden=false;btn.onclick=async()=>{try{const result=await api('/api/admin/stocktake/items',{method:'POST',body:JSON.stringify({...body,confirmNew:true})});status.textContent=`Added ${result.item.description}.`;btn.hidden=true;search.value='';form.reset();await load();}catch(e2){status.textContent=e2.message;}};}else status.textContent=err.message;}}
  form?.addEventListener('submit',addItem);
  document.getElementById('saveStocktake')?.addEventListener('click',async()=>{try{const entries=data.items.map(i=>({id:i.id,bar:i.counts?.bar||0,cellar:i.counts?.cellar||0,walk_in_fridge:i.counts?.walk_in_fridge||0}));status.textContent='Saving stock take…';const r=await api('/api/admin/stocktake/counts',{method:'PUT',body:JSON.stringify({entries})});status.textContent='Stock take saved.';data.updatedAt=r.updatedAt;meta.textContent=`${data.items.length} master products · Last saved ${new Date(r.updatedAt).toLocaleString()}`;}catch(e){status.textContent=e.message;}});
  document.getElementById('refreshStocktake')?.addEventListener('click',load);
  document.querySelector('[data-panel="stocktake"]')?.addEventListener('click',()=>setTimeout(load,0));
})();
