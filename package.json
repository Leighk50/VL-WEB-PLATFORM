{
  "name": "village-limits-platform",
  "version": "2.3.5",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node seed-sunday-menu.js && node seed-dessert-menu.js && node webex-inbound-adapter.js",
    "test": "node --check webex-inbound-adapter.js && node --check specials-sms-safe.js && node --check specials-print-entry.js && node --test enquiry-security.test.js enquiry-admin.test.js"
  }
}
