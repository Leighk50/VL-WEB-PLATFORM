# Admin enquiries

Successful website enquiry submissions are stored in the protected server data directory in `enquiries.json` and surfaced only through authenticated admin API routes. The public content API does not expose enquiry records.
